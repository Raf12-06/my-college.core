image: node:18.15.0

cache:
  paths:
    - node_modules

stages:
  - build
  - deploy

before_script:
  - npm install

build-job:
  stage: build
  only:
    - master
  artifacts:
    paths:
      - node_modules  
  script:
    - npm run build

deploy-job:
  stage: deploy
  only:
    - master
  script:
    - npm run start

